[Unit]
Description=GitHub Webhook Listener
After=network.target

[Service]
User=prod_user
ExecStart=/usr/bin/webhook \
  -hooks /home/prod_user/DJANGO_STARTER/infrastructure/hooks.json \
  -port 9000 \
  -verbose
Restart=always

[Install]
WantedBy=multi-user.target
